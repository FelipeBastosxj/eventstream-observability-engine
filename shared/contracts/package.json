{
  "name": "@eventstream/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical Event Model contracts and interfaces for the EventStream platform.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\""
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
