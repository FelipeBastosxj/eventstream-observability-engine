{
  "name": "@eventstream/utils",
  "version": "0.1.0",
  "private": true,
  "description": "Shared utility functions and helpers for EventStream services.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\""
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
