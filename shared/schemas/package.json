{
  "name": "@eventstream/schemas",
  "version": "0.1.0",
  "private": true,
  "description": "JSON Schema and validation definitions for the EventStream Canonical Event Model.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\""
  },
  "dependencies": {
    "@eventstream/contracts": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
